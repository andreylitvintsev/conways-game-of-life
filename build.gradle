plugins {
    id 'java'
    id 'idea'
    id 'application'
}

repositories {
    jcenter()
}

application {
    mainClassName = 'com.github.andreylitvintsev.gameoflife.Main'
}

jar {
    manifest {
        attributes "Main-Class": mainClassName
    }

    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}
